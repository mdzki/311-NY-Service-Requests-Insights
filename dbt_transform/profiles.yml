dbt_transform:
  outputs:
    dev:
      dataset: NY_311_SERVICE_REQUESTS_DATASET
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: /creds_json/gcp_key.json
      location: EU
      method: service-account
      priority: interactive
      project: ny-service-requests
      threads: 1
      type: bigquery
  target: dev
ny_311_transform:
  outputs:
    dev:
      dataset: NY_311_SERVICE_REQUESTS_DATASET
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: /creds_json/gcp_key.json
      location: EU
      method: service-account
      priority: interactive
      project: ny-service-requests
      threads: 1
      type: bigquery
  target: dev
